[project]
name = "erpnext_indonesia_localization"
authors = [
    { name = "Agile Technica", email = "info@agiletechnica.com"}
]
description = "Localization of ERPNext for Indonesia region, including Indonesia Tax and Charges feature"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=15.0.0" # Installed and managed by bench.
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"

[project.urls]
Repository = "https://github.com/zetrosofot/erpnext-indonesia-localization"
